run:
  name: best_vision_inductive_noisy_student_cifar10_R1-seed3
  seed: 3
  output_dir: /lustre/fsn1/projects/rech/dqp/uqj57xk/projet_modssc/outputs/R1/inductive/noisy_student/vision/cifar10/best_vision_inductive_noisy_student_cifar10_R1-sweep-20260330T011749Z
  log_level: detailed
  fail_fast: true
limits:
  profile: auto
dataset:
  id: cifar10
  download: true
  cache_dir: /lustre/fsn1/projects/rech/dqp/uqj57xk/projet_modssc/modssc_cache/datasets
  options: {}
sampling:
  seed: 3
  plan:
    split:
      kind: holdout
      test_fraction: 0.2
      val_fraction: 0.1
      stratify: true
      shuffle: true
    labeling:
      mode: per_class
      value: 1
      strategy: balanced
      min_per_class: 1
      per_class: true
      fixed_indices: null
    imbalance:
      kind: none
    policy:
      respect_official_test: true
      use_official_graph_masks: true
      allow_override_official: false
preprocess:
  seed: 3
  fit_on: train_labeled
  cache: true
  cache_dir: /lustre/fsn1/projects/rech/dqp/uqj57xk/projet_modssc/modssc_cache/preprocess
  plan:
    output_key: features.X
    steps:
    - id: labels.encode
    - id: labels.to_torch
      params:
        device: auto
        dtype: int64
    - id: vision.ensure_num_channels
      params:
        num_channels: 3
    - id: vision.channels_order
      params:
        order: NCHW
    - id: vision.resize
      params:
        height: 32
        width: 32
    - id: vision.normalize
      params:
        mean:
        - 123.675
        - 116.28
        - 103.53
        std:
        - 58.395
        - 57.12
        - 57.375
    - id: core.copy_raw
augmentation:
  enabled: true
  seed: 3
  mode: fixed
  modality: vision
  weak:
    steps:
    - id: vision.random_crop_pad
      params:
        padding: 4
    - id: vision.random_horizontal_flip
  strong:
    steps:
    - id: vision.random_crop_pad
      params:
        padding: 4
    - id: vision.random_horizontal_flip
    - id: vision.cutout
      params:
        frac: 0.25
method:
  kind: inductive
  id: noisy_student
  device:
    device: auto
    dtype: float32
    resolved_device: cuda
  params:
    lambda_u: 1.0
    p_cutoff: 0.95
    temperature: 0.5
    hard_label: true
    use_cat: false
    batch_size: 128
    max_epochs: 50
    detach_target: true
  model:
    classifier_id: image_pretrained
    classifier_backend: torch
    classifier_params:
      model_name: resnet18
      weights: null
      freeze_backbone: false
      input_layout: channels_first
      lr: 0.0001
      weight_decay: 0.0001
      batch_size: 32
      max_epochs: 5
    ema: false
evaluation:
  split_for_model_selection: val
  report_splits:
  - val
  - test
  metrics:
  - accuracy
  - macro_f1
