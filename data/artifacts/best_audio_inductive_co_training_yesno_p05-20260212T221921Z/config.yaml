run:
  name: best_audio_inductive_co_training_yesno_p05
  seed: 4
  output_dir: /home/mbarbaux/scratch/ModSSC/runs/p05/inductive/co_training/audio/yesno
  fail_fast: true
  log_level: detailed
limits:
  profile: auto
dataset:
  id: yesno
  download: true
  cache_dir: /home/mbarbaux/scratch/ModSSC/cache/datasets
  options: {}
sampling:
  seed: 4
  plan:
    split:
      kind: holdout
      test_fraction: 0.2
      val_fraction: 0.1
      stratify: true
      shuffle: true
    labeling:
      mode: fraction
      value: 0.05
      strategy: balanced
      min_per_class: 1
    imbalance:
      kind: none
    policy:
      respect_official_test: true
      use_official_graph_masks: true
      allow_override_official: false
preprocess:
  seed: 4
  fit_on: train_labeled
  cache: true
  cache_dir: /home/mbarbaux/scratch/ModSSC/cache/preprocess
  plan:
    output_key: features.X
    steps:
    - id: labels.encode
    - id: labels.to_torch
      params:
        device: auto
        dtype: int64
    - id: audio.load_waveform
      params:
        allow_fallback: true
        target_sample_rate: 16000
        max_length: 16000
    - id: core.to_torch
      params:
        device: auto
        dtype: float32
method:
  kind: inductive
  id: co_training
  device:
    device: auto
    dtype: float32
    resolved_device: cuda
  params:
    classifier_id: audio_pretrained
    classifier_backend: torch
    classifier_params:
      bundle: WAV2VEC2_BASE
      freeze_backbone: false
      lr: 0.0001
      weight_decay: 0.0001
      batch_size: 8
      max_epochs: 5
    max_iter: 20
    k_per_class: 1
evaluation:
  split_for_model_selection: val
  report_splits:
  - val
  - test
  metrics:
  - accuracy
  - macro_f1
views:
  seed: 4
  plan:
    views:
    - name: view_a
      columns:
        mode: random
        fraction: 0.5
    - name: view_b
      columns:
        mode: complement
        complement_of: view_a
