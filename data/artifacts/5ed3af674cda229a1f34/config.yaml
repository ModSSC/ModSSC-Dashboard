run:
  name: best_audio_inductive_deep_co_training_yesno_R2-seed6
  seed: 6
  output_dir: /lustre/fsn1/projects/rech/dqp/uqj57xk/projet_modssc/outputs/R2/inductive/deep_co_training/audio/yesno/best_audio_inductive_deep_co_training_yesno_R2-sweep-20260329T001853Z
  fail_fast: true
  log_level: detailed
limits:
  profile: auto
dataset:
  id: yesno
  download: true
  cache_dir: /lustre/fsn1/projects/rech/dqp/uqj57xk/projet_modssc/modssc_cache/datasets
  options: {}
sampling:
  seed: 6
  plan:
    split:
      kind: holdout
      test_fraction: 0.2
      val_fraction: 0.1
      stratify: true
      shuffle: true
    labeling:
      mode: per_class
      value: 3
      strategy: balanced
      min_per_class: 1
      per_class: true
      fixed_indices: null
    imbalance:
      kind: none
    policy:
      respect_official_test: true
      use_official_graph_masks: true
      allow_override_official: false
preprocess:
  seed: 6
  fit_on: train_labeled
  cache: true
  cache_dir: /lustre/fsn1/projects/rech/dqp/uqj57xk/projet_modssc/modssc_cache/preprocess
  plan:
    output_key: features.X
    steps:
    - id: labels.encode
    - id: labels.to_torch
      params:
        device: auto
        dtype: int64
    - id: audio.load_waveform
      params:
        allow_fallback: true
        target_sample_rate: 16000
        max_length: 16000
    - id: core.to_torch
      params:
        device: auto
        dtype: float32
augmentation:
  enabled: true
  seed: 6
  mode: fixed
  modality: audio
  weak:
    steps:
    - id: audio.add_noise
      params:
        std: 0.002
  strong:
    steps:
    - id: audio.add_noise
      params:
        std: 0.005
    - id: audio.time_shift
      params:
        max_frac: 0.1
method:
  kind: inductive
  id: deep_co_training
  device:
    device: auto
    dtype: float32
    resolved_device: cuda
  params:
    lambda_cot: 1.0
    lambda_dif: 1.0
    adv_eps: 0.03
    batch_size: 128
    max_epochs: 50
    freeze_bn: true
    detach_target: true
  model:
    classifier_id: audio_pretrained
    classifier_backend: torch
    classifier_params:
      bundle: WAV2VEC2_BASE
      freeze_backbone: false
      lr: 0.0001
      weight_decay: 0.0001
      batch_size: 8
      max_epochs: 5
    ema: false
evaluation:
  split_for_model_selection: val
  report_splits:
  - val
  - test
  metrics:
  - accuracy
  - macro_f1
